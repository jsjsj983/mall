spring:
  profiles:
    active: dev
  application:
    name: mall-admin
  cloud:
      sentinel:
        transport:
          dashboard: localhost:8333
          port: 8888
        # 服务启动直接建立心跳连接
        eager: true